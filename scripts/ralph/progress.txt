# Ralph Progress Log
Started: dom 15 mar 2026 17:02:50 -03

## Codebase Patterns
- Document reusable codebase patterns found during iterations.
- Centralize visual foundations in `src/app/styles/index.css` and treat `surface-base`, `surface-panel`, `surface-overlay`, `surface-shell`, `surface-interactive` as the only shared surface contracts.
- Keep shared feedback semantic by routing toast/dialog states through `feedback-*` classes and theme tokens instead of hardcoded red/green/blue Tailwind palettes.
- Avoid default glassmorphism in shared primitives; blur, glow and strong gradients should be opt-in accents for spotlight moments, not the baseline container treatment.
- Keep shell information architecture in a single shared navigation hook (`src/shared/components/navigation/use-shell-navigation.ts`) so mobile sheets, desktop sidebar and topbar context cannot drift apart.
- In the mobile shell, recurring flows stay on the fixed bottom bar while secondary/admin routes move to labeled sheets; safe-area padding belongs to the shell contract, not to individual pages.
- On timer-driven screens, memoize non-live panels and invalidate expensive derived stats with a solve/session signature instead of recomputing them on every animation frame.
- Analytics screens should expose period/metric controls as segmented buttons near the section header and degrade dense tables into card rows before the `lg` breakpoint.
- Learning-oriented routes stay aligned longer when their cross-navigation lives in one shared header action cluster instead of duplicating route links inside each page.
- Support surfaces stay coherent when standalone/auth/share pages reuse the same branded shell and when setup/login/empty/error states are promoted to first-class cards instead of inline warning text.

---
## 2026-03-15 17:33:34 -0300 - US-018
- Implemented the mobile-first redesign foundations by refreshing global tokens for color, type, spacing, radius, elevation and motion in `src/app/styles/index.css`.
- Standardized shared surfaces and refreshed the most-used shared primitives: `Button`, `Card`, `Modal`, `DropdownMenu`, `PageHeader`, `Topbar`, `Sidebar`, `MobileNav`, `Toast` and `ConfirmDialog`.
- Updated `docs/technical.md` to document the new surface contract, theme direction and redesign anti-patterns.
- Validated the story with `pnpm lint`, `pnpm test`, `pnpm knip` and `pnpm build`.
- Files changed:
  - `src/app/styles/index.css`
  - `src/shared/components/ui/button/button.tsx`
  - `src/shared/components/ui/card/card.tsx`
  - `src/shared/components/ui/modal/modal.tsx`
  - `src/shared/components/ui/dropdown-menu/dropdown-menu.tsx`
  - `src/shared/components/ui/radio-button/radio-button.tsx`
  - `src/shared/components/ui/toggle-button/toggle-button.tsx`
  - `src/shared/components/page-header/page-header.tsx`
  - `src/shared/components/navigation/topbar.tsx`
  - `src/shared/components/navigation/sidebar.tsx`
  - `src/shared/components/navigation/mobile-nav.tsx`
  - `src/shared/components/toast/toast.tsx`
  - `src/shared/components/toast/use-toast.ts`
  - `src/shared/components/confirm-dialog/confirm-dialog.tsx`
  - `docs/technical.md`
- Learnings for future iterations:
  - Surface cleanup has to happen at the CSS contract level first; updating `Card` alone is not enough because many routes use `surface-panel` directly.
  - `DropdownMenuTriggerButton`, `SessionSwitcher` and `LanguageSelector` are part of the shell visual contract and should evolve together in any navigation redesign.
  - `US-019` can reuse the new shell/focus/surface tokens without reintroducing hamburger-era glow and blur treatments.
---
## 2026-03-15 18:02:08 -0300 - US-019
- Implemented the mobile-first shell redesign by replacing the hamburger-first mobile nav with a fixed bottom navigation plus `Aprender` and `Mais` sheets fed by a shared navigation contract.
- Refined `Sidebar`, `Topbar`, `SessionSwitcher`, `PuzzleSelector` and `PageHeader` so the global shell now shares one information architecture and one control density across breakpoints.
- Added shell safe-area handling in `src/app/styles/index.css` for topbar, bottom nav and main scroll region, preserving content-only scrolling inside the app shell.
- Updated `docs/technical.md` to document the final shell contract and route grouping used by the redesign.
- Ran QA in mobile and desktop using Firefox headless screenshots (`390x844` home + sheets, `1440x1024` history) before closing the story.
- Validated the story with `pnpm lint`, `pnpm test`, `pnpm knip` and `pnpm build`.
- Files changed:
  - `src/shared/components/navigation/use-shell-navigation.ts`
  - `src/shared/components/navigation/mobile-nav.tsx`
  - `src/shared/components/navigation/sidebar.tsx`
  - `src/shared/components/navigation/topbar.tsx`
  - `src/shared/layouts/main-layout.tsx`
  - `src/shared/components/page-header/page-header.tsx`
  - `src/app/styles/index.css`
  - `src/shared/config/i18n/locales/pt-BR.ts`
  - `src/shared/config/i18n/locales/en-US.ts`
  - `src/shared/config/i18n/locales/es-ES.ts`
  - `docs/technical.md`
  - `scripts/ralph/prd.json`
- Learnings for future iterations:
  - The shell should own safe-area padding centrally; route pages only need `app-shell-page` and should not guess bottom/nav offsets.
  - Grouped route metadata is worth keeping as a shared contract because mobile and desktop IA now evolve from the same source instead of duplicated link arrays.
  - Bottom navigation needs a more solid surface than the rest of the shell to avoid content bleed-through on dense home layouts.
---
## 2026-03-15 18:43:05 -0300 - US-020
- Rebuilt the home around the timer core loop by removing the hero-style intro, integrating primary actions into the timer panel and moving stats/progress/history into a secondary section.
- Refined the scramble area with clearer 3D/2D switching, compact preview sizing and copy/regenerate controls that stay lightweight on mobile.
- Reduced timer-loop overhead by memoizing non-live home panels and caching session stats behind a solve/session signature so the 3D scramble panel no longer re-renders on every animation frame.
- Updated `docs/implementation.md` and `docs/changelog.md` to reflect the redesigned home component set and the new mobile-first hierarchy.
- Validated the story with `pnpm lint`, `pnpm test`, `pnpm knip` and `pnpm build`, plus Playwright CLI screenshots at `390x844` and `1440x1024` using blocked service workers and onboarding-disabled storage.
- Files changed:
  - `src/features/home/home.tsx`
  - `src/features/home/hooks/use-home-timer-dashboard.ts`
  - `src/features/home/components/home-timer-panel.tsx`
  - `src/features/home/components/home-controls.tsx`
  - `src/features/home/components/home-scramble-panel.tsx`
  - `src/features/home/components/home-stats-grid.tsx`
  - `src/features/home/components/home-progress-panel.tsx`
  - `src/features/home/components/home-solve-feed.tsx`
  - `src/shared/config/i18n/locales/pt-BR.ts`
  - `src/shared/config/i18n/locales/en-US.ts`
  - `src/shared/config/i18n/locales/es-ES.ts`
  - `docs/implementation.md`
  - `docs/changelog.md`
  - `scripts/ralph/prd.json`
- Learnings for future iterations:
  - Mobile-first fold targets are easy to miss if action grids silently collapse to one column; enforce base grid columns explicitly instead of relying on `sm:` breakpoints.
  - PWA/offline prompts can invalidate visual QA for critical first-fold flows, so screenshot runs should block service workers or preload a storage state that suppresses transient overlays.
  - Compacting first-fold support cards is usually cheaper than shrinking the timer digits; preserve timer dominance and reclaim space from secondary explanatory blocks first.
---
## 2026-03-15 19:06:42 -0300 - US-021
- Refined `history` with objective summary cards, a calmer session chart container, segmented responsive filters and a solve list that stays readable as cards on mobile and a table on desktop.
- Rebuilt solve details to follow the redesign contract: bottom sheet on mobile, centered modal on desktop, lighter surfaces and direct penalty actions without reintroducing glass-heavy styling.
- Reworked `stats` around insight modules with period/métrica segmented controls, overview cards and lower-noise charts, including a vertical distribution chart for mobile legibility.
- Added a shared `useMediaQuery` hook for responsive behavior in analytics/detail surfaces and updated locale strings plus implementation/changelog docs to reflect the new contracts.
- Validated the story with `pnpm lint`, `pnpm test`, `pnpm build` and `pnpm knip`.
- Files changed:
  - `src/features/history/history.tsx`
  - `src/features/history/components/summary-cards/summary-cards.tsx`
  - `src/features/history/components/summary-cards/use-summary-cards.ts`
  - `src/features/history/components/history-charts/history-charts.tsx`
  - `src/features/history/components/solve-table/solve-table.tsx`
  - `src/features/history/components/solve-table/use-solve-table.ts`
  - `src/features/history/components/solve-details-modal/solve-details-modal.tsx`
  - `src/features/history/components/solve-details-modal/use-solve-details-modal.ts`
  - `src/features/stats/stats.tsx`
  - `src/features/stats/components/advanced-stats/advanced-stats-content.tsx`
  - `src/features/stats/components/advanced-stats/evolution-chart.tsx`
  - `src/features/stats/components/advanced-stats/distribution-chart.tsx`
  - `src/features/stats/components/advanced-stats/use-advanced-stats-modal.ts`
  - `src/shared/hooks/use-media-query.ts`
  - `src/shared/hooks/index.ts`
  - `src/shared/config/i18n/locales/pt-BR.ts`
  - `src/shared/config/i18n/locales/en-US.ts`
  - `src/shared/config/i18n/locales/es-ES.ts`
  - `docs/implementation.md`
  - `docs/changelog.md`
  - `scripts/ralph/prd.json`
- Learnings for future iterations:
  - History/details flows are easier to keep coherent when the responsive switch lives in the feature hook and the content body stays shared between modal and sheet renderers.
  - Analytics charts need explicit series/period controls in the page chrome; relying only on chart legends is not enough for one-hand mobile exploration.
  - Vertical histograms are a better default than rotated x-axis labels for mobile data distribution views in this codebase.
---
## 2026-03-15 19:45:17 -0300 - US-022
- Unified `training`, `tutorial` and `cube-3d` around one learning-surface contract with shared header actions, calmer overview cards and viewer-first mobile layouts.
- Rebuilt `/training` around the active drill, overall progress and next-step guidance, while preserving quick attempt/confidence actions and the shared `CubePlatform` step-by-step experience.
- Refined `/tutorial` into a guided CFOP journey with stage roadmap, lesson list, clearer recognition/checklist blocks and explicit replay/next progression.
- Integrated `/cube-3d` back into the app shell via `use-cube-workspace`, workspace status cards, grouped quick actions and collapsible move history instead of the legacy demo-style overlay.
- Updated `docs/implementation.md`, `docs/technical.md`, `docs/changelog.md` and `scripts/ralph/prd.json` to document and mark the story complete.
- Validated the story with `pnpm lint`, `pnpm test`, `pnpm knip` and `pnpm build`.
- Files changed:
  - `src/features/training/training.tsx`
  - `src/features/training/hooks/use-training-lab.ts`
  - `src/features/training/components/training-drill-panel.tsx`
  - `src/features/training/components/training-drill-list.tsx`
  - `src/features/tutorial/tutorial.tsx`
  - `src/features/tutorial/hooks/use-tutorial-guide.ts`
  - `src/features/cube-3d/cube-3d.tsx`
  - `src/features/cube-3d/hooks/use-cube-workspace.ts`
  - `src/features/cube-3d/components/cube-action-bar.tsx`
  - `src/features/cube-3d/components/move-history.tsx`
  - `src/features/cube-3d/components/move-indicator.tsx`
  - `src/features/cube-3d/components/theme-selector.tsx`
  - `src/shared/components/learning-surface-actions/learning-surface-actions.tsx`
  - `src/shared/components/learning-surface-actions/index.ts`
  - `src/shared/components/cube-platform/cube-playback-controls.tsx`
  - `src/shared/config/i18n/locales/pt-BR.ts`
  - `src/shared/config/i18n/locales/en-US.ts`
  - `src/shared/config/i18n/locales/es-ES.ts`
  - `src/shared/index.ts`
  - `docs/implementation.md`
  - `docs/technical.md`
  - `docs/changelog.md`
  - `scripts/ralph/prd.json`
- Learnings for future iterations:
  - A single shared action cluster for related routes is cheaper to maintain than mirroring route links in each learning page.
  - Viewer-heavy educational flows read better on mobile when overview and navigation live in secondary cards, leaving the 3D surface dominant in the primary column.
  - Wrapping page-specific cube orchestration in a dedicated hook (`use-cube-workspace`) keeps scene state, status copy and audio/history behavior out of the render layer.
---
## 2026-03-15 20:16:00 -0300 - US-023
- Redesigned `/friends` and `/leaderboard` with summary cards, clearer setup/login states and responsive social listings that keep actions direct on mobile.
- Reorganized `settings` by extracting import/export logic into `useSettingsContent` and regrouping experience, sync, sharing and backup surfaces with clearer hierarchy.
- Rebuilt `/share/:slug` and `/auth/callback` as branded standalone pages with explicit loading, unavailable and error states instead of exception-style layouts.
- Polished auxiliary overlays (`OnboardingTooltip`, `PWAUpdatePrompt`, `SessionManagerModal`) so onboarding, offline/update prompts and session management now follow the same surface contract.
- Validated the story with `pnpm lint`, `pnpm test`, `pnpm knip` and `pnpm build`.
- Files changed:
  - `src/features/friends/friends.tsx`
  - `src/features/friends/components/friend-invite-form.tsx`
  - `src/features/friends/components/friend-invites-list.tsx`
  - `src/features/friends/components/friends-list.tsx`
  - `src/features/leaderboard/leaderboard.tsx`
  - `src/features/leaderboard/components/leaderboard-period-switch.tsx`
  - `src/features/leaderboard/components/leaderboard-table.tsx`
  - `src/features/settings/components/settings-content/index.ts`
  - `src/features/settings/components/settings-content/settings-content.tsx`
  - `src/features/settings/components/settings-content/use-settings-content.ts`
  - `src/features/settings/components/cloud-sync-section/cloud-sync-section.tsx`
  - `src/features/settings/components/share-section/share-section.tsx`
  - `src/features/share/share.tsx`
  - `src/features/auth-callback/auth-callback.tsx`
  - `src/shared/components/onboarding/onboarding-tooltip.tsx`
  - `src/shared/components/pwa-update-prompt/pwa-update-prompt.tsx`
  - `src/shared/components/session-manager-modal/session-manager-modal.tsx`
  - `src/shared/config/i18n/locales/pt-BR.ts`
  - `src/shared/config/i18n/locales/en-US.ts`
  - `src/shared/config/i18n/locales/es-ES.ts`
  - `docs/implementation.md`
  - `docs/changelog.md`
  - `scripts/ralph/prd.json`
- Learnings for future iterations:
  - Social/support pages read faster when readiness states include the next action inline instead of leaving navigation back to the user.
  - Extracting page-local orchestration into a collocated hook is enough to keep a wide settings screen maintainable without inventing a new shared abstraction.
  - Public/standalone routes feel less like exceptions when they reuse the same brand shell as auth and sharing rather than rolling bespoke full-screen cards.
---
