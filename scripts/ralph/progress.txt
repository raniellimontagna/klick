# Ralph Progress Log
Started: 2026-03-13

## Codebase Patterns
- Projeto single-app com estrutura feature-based em `src/features`.
- Código compartilhado e design primitives vivem em `src/shared`.
- Estado global centralizado em stores Zustand com persistência local.
- Rotas principais são lazy-loaded via `src/app/app-router.tsx`.
- Regras e contexto de produto ficam em `PROJECT.md` e `docs/technical.md`.
- Quality gate mínimo antes de commit: `pnpm lint` e `pnpm test`.
- Plataforma de cubo 3D reutilizável foi centralizada em `src/shared` com camadas separadas: `lib/cube-platform` (motor), `hooks/use-cube-platform-*` (controle/estado) e `components/cube-platform` (render/integração).
- Para escalar puzzles 3D (2x2/3x3/4x4), manter `puzzles/*` como source of truth para dimensão, camadas e move map evita regras hardcoded no controller/render.
- Para fluxos didáticos, modelar conteúdo em `lib` (método/etapa/lição) e manter textos em namespace i18n próprio evita acoplamento entre UI e conteúdo.
- Para reescrita greenfield de feature, remover o legado da pasta da feature e reconstruir com núcleo orientado a dados (`lib`), orquestração em hook e renderização desacoplada em componentes.
- Em telas com timer crítico, desacoplar estado/atalhos em hook único e pausar visualizações pesadas durante execução melhora fluidez sem perder recursos visuais.
- Para sync offline-first com Supabase, manter stores locais como source of truth de runtime e sincronizar por merge `last-write-wins` usando `updated_at` reduz acoplamento entre UI e backend.
- Para RLS consistente, espelhar a regra `auth.uid() = user_id` em `SELECT/INSERT/UPDATE/DELETE` em todas as tabelas de usuário evita vazamento cross-account no client SDK.

---
## [2026-03-13 00:00:00 -0300] - US-001
- What was implemented
  - Estrutura inicial do Ralph criada em `scripts/ralph`.
  - Script de loop `ralph.sh` adicionado com suporte para `codex`, `claude` e `amp`.
  - Instruções específicas para agentes adicionadas em `CODEX.md` e `CLAUDE.md`.
  - PRD inicial do Klick criado com contexto técnico, regras e story baseline.
  - Fallback de regras no loop configurado para `PROJECT.md` e documentação técnica.
- Files changed
  - scripts/ralph/ralph.sh
  - scripts/ralph/CODEX.md
  - scripts/ralph/CLAUDE.md
  - scripts/ralph/prd.json
  - scripts/ralph/progress.txt
- Learnings for future iterations:
  - O Klick não possui `CLAUDE.md`/`.clinerules` na raiz, então o Ralph deve consumir regras diretamente do `PROJECT.md`.
  - `docs/technical.md` complementa arquitetura e padrões de implementação que impactam decisões de código.
---
## [2026-03-13 16:29:54 -0300] - US-004
- What was implemented
  - Núcleo do cubo 3D extraído de `src/features/cube-3d` para módulos compartilhados em `src/shared` (estado, engine de movimentos, renderização e tema).
  - API de integração reutilizável criada com `CubePlatform` (props de `cubeType`, `algorithm`, `interactive`, `keyboardEnabled`, `themeId` e callbacks) e hook `useCubePlatformController`.
  - Página `/cube-3d` migrada para consumir apenas a plataforma compartilhada (`CubePlatformScene`, controller e keyboard hook), eliminando acoplamento da lógica 3D ao módulo de rota.
- Files changed
  - src/shared/lib/cube-platform/*
  - src/shared/hooks/use-cube-platform-controller.ts
  - src/shared/hooks/use-cube-platform-interaction.ts
  - src/shared/hooks/use-cube-platform-keyboard.ts
  - src/shared/components/cube-platform/*
  - src/shared/store/cube-platform-theme-store.ts
  - src/features/cube-3d/cube-3d.tsx
  - src/features/cube-3d/components/theme-selector.tsx
  - src/shared/index.ts
  - src/shared/hooks/index.ts
  - src/shared/lib/index.ts
  - src/shared/store/index.ts
- Learnings for future iterations:
  - Como integrar cubo 3D em uma nova tela com baixo acoplamento:
    - Para uso rápido: renderizar `<CubePlatform algorithm=\"...\" interactive keyboardEnabled />`.
    - Para tela com controles próprios: usar `useCubePlatformController(...)` + `CubePlatformScene` e conectar botões/atalhos às actions do controller.
    - Para tema: usar `themeId` no `CubePlatform` ou acessar `useCubePlatformThemeStore` em componentes de customização.
  - O split em camadas facilita evolução para múltiplos puzzles (2x2/4x4) sem alterar consumidores de rota.
---
## [2026-03-13 16:44:10 -0300] - US-002
- What was implemented
  - Reescrita greenfield completa da feature `/tutorial` com nova arquitetura modular em `src/features/tutorial` (hook de fluxo + catálogo de lições + página de apresentação).
  - Implementado fluxo CFOP em 4 etapas (Cross, F2L, OLL, PLL) com lições guiadas, checklist, reconhecimento de casos e progressão etapa/lição.
  - Integração didática com cubo 3D reutilizável via `CubePlatform`, incluindo replay de caso por lição.
  - Novo namespace i18n `tutorialGuide` adicionado em `pt-BR`, `en-US` e `es-ES`, sem reutilizar o conjunto de chaves legado de tutorial.
  - Implementação legada removida antes da nova (store e componentes antigos de tutorial).
- Files changed
  - Removed (legacy):
    - src/features/tutorial/tutorial-store.ts
    - src/features/tutorial/components/tutorial-content/tutorial-content.tsx
    - src/features/tutorial/components/tutorial-content/tutorial-visualizations.tsx
    - src/features/tutorial/components/tutorial-content/use-tutorial-content.ts
  - Added (greenfield):
    - src/features/tutorial/hooks/use-tutorial-guide.ts
    - src/features/tutorial/lib/tutorial-guide-model.ts
  - Recreated/updated:
    - src/features/tutorial/tutorial.tsx
    - src/features/tutorial/index.ts
    - src/shared/config/i18n/locales/pt-BR.ts
    - src/shared/config/i18n/locales/en-US.ts
    - src/shared/config/i18n/locales/es-ES.ts
    - scripts/ralph/prd.json
    - scripts/ralph/progress.txt
- Learnings for future iterations:
  - Estruturar lições por dados (`setupAlgorithm` + `solveAlgorithm`) facilita troca de conteúdo e expansão para novos métodos sem mexer na UI.
  - A combinação `useTutorialGuide` + `tutorialGuide` (i18n) mantém separação clara entre estado de navegação e texto/copy da feature.
  - `pnpm knip` permanece com falhas pré-existentes fora do escopo da US-002; `pnpm lint`, `pnpm test` e `pnpm build` passaram após a reescrita.
---
## [2026-03-13 16:57:41 -0300] - US-003
- What was implemented
  - Reescrita greenfield completa da aba `/training` com nova arquitetura modular (`lib` catálogo de drills, `store` de progresso, `hooks` de orquestração e `components` de apresentação).
  - Nova experiência de treino criada do zero com navegação por trilhas (F2L/OLL/PLL), seleção de drills e painel de detalhes com progresso por caso.
  - Integração visual com cubo 3D reutilizável via `CubePlatform`, reproduzindo setup + algoritmo para cada drill com replay dedicado.
  - Novo namespace i18n `trainingLab` adicionado em `pt-BR`, `en-US` e `es-ES`, substituindo o conjunto legado `training`.
  - Persistência do progresso refeita com Zustand (`klick-training-lab-v2`) e merge resiliente para suportar expansão futura de categorias/casos por dados.
- Files changed
  - Removed (legacy):
    - src/features/training/cases.ts
    - src/features/training/components/training-case-card.tsx
    - src/features/training/lib/training-data/cases.ts
    - src/features/training/lib/training-data/types.ts
    - src/features/training/training-store.ts
    - src/features/training/types.ts
    - src/features/training/visualizations.tsx
  - Added (greenfield):
    - src/features/training/lib/training-lab-types.ts
    - src/features/training/lib/training-lab-catalog.ts
    - src/features/training/store/training-lab-store.ts
    - src/features/training/hooks/use-training-lab.ts
    - src/features/training/components/training-drill-list.tsx
    - src/features/training/components/training-drill-panel.tsx
    - src/features/training/components/index.ts
  - Recreated/updated:
    - src/features/training/training.tsx
    - src/features/training/index.ts
    - src/shared/config/i18n/locales/pt-BR.ts
    - src/shared/config/i18n/locales/en-US.ts
    - src/shared/config/i18n/locales/es-ES.ts
    - scripts/ralph/prd.json
    - scripts/ralph/progress.txt
- Learnings for future iterations:
  - Um catálogo com `trackDefinitions + drills` e IDs estáveis permite adicionar novos casos sem alterar a UI core.
  - Calcular `setupAlgorithm` como inverso do `solveAlgorithm` reduz manutenção manual e gera demonstração 3D reproduzível.
  - `pnpm lint`, `pnpm test` e `pnpm build` passaram; `pnpm knip` continua com itens pré-existentes fora do escopo da US-003.
---
## [2026-03-15 10:56:56 -0300] - US-005
- What was implemented
  - Reescrita greenfield da `/` com nova composição visual: hero orientado a iniciantes, painel do timer, painel de scramble e histórico rápido na mesma tela.
  - Novo fluxo de visualização do scramble com alternância explícita entre 3D (`CubePlatform`) e 2D (`CubeVisualizer`).
  - Fluxos essenciais preservados e reforçados: timer com Space, inspeção, registro automático de solve, atalhos `N/P/D/U`, botões de +2/DNF/desfazer e cards de estatísticas (single/ao5/ao12/best).
  - Ajuste de robustez nos atalhos globais para bloquear interações quando inputs/select/contentEditable/modal estiverem ativos.
  - Novo namespace i18n `homeRevamp` criado em `pt-BR`, `en-US` e `es-ES` para os textos da home refeita.
- Files changed
  - Removed (legacy):
    - src/features/home/use-home.ts
    - src/features/home/components/home-page-components/stat-card.tsx
    - src/features/home/components/home-page-components/stats-info-modal.tsx
    - src/features/home/components/inspection-display/inspection-display.tsx
    - src/features/home/components/scramble-box/scramble-box.tsx
    - src/features/home/components/scramble-box/use-scramble-box.ts
    - src/features/home/components/timer-display/timer-display.tsx
  - Added (greenfield):
    - src/features/home/hooks/use-home-timer-dashboard.ts
    - src/features/home/lib/keyboard-shortcuts.ts
    - src/features/home/components/home-timer-panel.tsx
    - src/features/home/components/home-scramble-panel.tsx
    - src/features/home/components/home-controls.tsx
    - src/features/home/components/home-stats-grid.tsx
    - src/features/home/components/home-solve-feed.tsx
    - src/features/home/components/index.ts
  - Recreated/updated:
    - src/features/home/home.tsx
    - src/features/home/index.ts
    - src/features/home/lib/use-timer.ts
    - src/shared/config/i18n/locales/pt-BR.ts
    - src/shared/config/i18n/locales/en-US.ts
    - src/shared/config/i18n/locales/es-ES.ts
    - scripts/ralph/prd.json
    - scripts/ralph/progress.txt
- Learnings for future iterations:
  - Para home com timer em tempo real, pausar renderização pesada durante `running/inspection` reduz risco de jitter perceptível.
  - Centralizar atalhos em um guard utilitário (`shouldIgnoreGlobalShortcut`) evita conflito com modais e campos editáveis sem duplicar regra em cada hook.
  - `pnpm lint` e `pnpm test` passaram; `pnpm knip` falhou por ausência do comando no projeto (`Command "knip" not found`).
---
## [2026-03-15 11:10:11 -0300] - US-006
- What was implemented
  - Suporte real a `cubeType` (`2x2`, `3x3`, `4x4`) implementado no core da plataforma 3D com módulos isolados por puzzle em `src/shared/lib/cube-platform/puzzles`.
  - Motor de estado atualizado para usar definição de puzzle (dimensão, camadas e move map) em `createSolvedCube`/`applyMove`/`applyAlgorithm`, removendo fallback fixo para 3x3 no controller.
  - Interação por drag adaptada para grades pares e ímpares via resolução por camada (`positive/middle/negative`), mantendo middle-slices apenas quando o puzzle suporta.
  - Renderização normalizada por tipo de puzzle com `renderScale` para manter legibilidade visual consistente no `CubePlatformScene`.
  - Testes adicionados para regras de movimentos por puzzle e integração do controller por tipo (`2x2/3x3/4x4`).
- Files changed
  - Added:
    - src/shared/lib/cube-platform/puzzles/create-puzzle-definition.ts
    - src/shared/lib/cube-platform/puzzles/puzzle-definition.ts
    - src/shared/lib/cube-platform/puzzles/puzzle-2x2.ts
    - src/shared/lib/cube-platform/puzzles/puzzle-3x3.ts
    - src/shared/lib/cube-platform/puzzles/puzzle-4x4.ts
    - src/shared/lib/cube-platform/puzzles/index.ts
    - src/shared/lib/cube-platform/puzzles.test.ts
    - src/shared/hooks/use-cube-platform-controller.test.ts
  - Updated:
    - src/shared/lib/cube-platform/moves.ts
    - src/shared/lib/cube-platform/cube-utils.ts
    - src/shared/lib/cube-platform/index.ts
    - src/shared/lib/cube-platform/types.ts
    - src/shared/hooks/use-cube-platform-controller.ts
    - src/shared/hooks/use-cube-platform-interaction.ts
    - src/shared/components/cube-platform/cube-scene.tsx
    - src/shared/components/cube-platform/cube-platform.tsx
    - src/features/cube-3d/cube-3d.tsx
    - scripts/ralph/prd.json
    - scripts/ralph/progress.txt
- Learnings for future iterations:
  - Manter `moveMap` específico por puzzle simplifica suporte incremental a novos tipos (ex.: 5x5) sem refatorar controller/hook.
  - `renderScale` no nível da definição de puzzle reduz ajustes manuais de câmera quando o tamanho da grade muda.
  - `pnpm lint` e `pnpm test` passaram; `pnpm knip` falhou por ausência de comando (`Command "knip" not found`).
---
## [2026-03-15 11:33:48 -0300] - US-008
- What was implemented
  - Integração Supabase adicionada com configuração por ambiente (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`), client browser e fallback automático para modo local quando não configurado.
  - Fluxo de autenticação implementado com Google OAuth (prioritário), fallback por email magic link, logout, sessão persistida e callback seguro em `/auth/callback` com sanitização de redirecionamento.
  - Motor de sincronização bidirecional implementado em `src/shared/lib/supabase/sync.ts` com estratégia `last-write-wins` por `updated_at`, incluindo merge local+nuvem no primeiro login.
  - Stores locais evoluídas para suportar sincronização: `sessions-store` com metadados (`createdAt`/`updatedAt`) e hidratação remota; `settings-store` com `updatedAt` e `hydrateSettings`.
  - Bootstrap global de auth/sync adicionado via `useAuthBootstrap` no `App`, incluindo re-sync automático quando online e em mudanças de dados locais.
  - Nova seção de Conta e Sync em Configurações com status de nuvem, login Google, magic link, sincronização manual, logout e feedback de erro/sucesso.
  - Novas migrations Supabase criadas para `profiles`, `sessions`, `solves`, `user_settings` com índices, triggers de `updated_at` e políticas RLS de isolamento por usuário.
  - Documentação operacional adicionada em `docs/supabase-migrations.md` e `.env.example` para setup local.
- Files changed
  - Added:
    - src/shared/lib/supabase/client.ts
    - src/shared/lib/supabase/env.ts
    - src/shared/lib/supabase/database.ts
    - src/shared/lib/supabase/sync.ts
    - src/shared/lib/supabase/sync.test.ts
    - src/shared/store/auth-store.ts
    - src/shared/hooks/use-auth-bootstrap.ts
    - src/features/auth-callback/auth-callback.tsx
    - src/features/auth-callback/use-auth-callback.ts
    - src/features/auth-callback/index.ts
    - src/features/settings/components/cloud-sync-section/cloud-sync-section.tsx
    - src/features/settings/components/cloud-sync-section/use-cloud-sync-section.ts
    - src/features/settings/components/cloud-sync-section/index.ts
    - supabase/migrations/20260315123000_us008_auth_sync.sql
    - docs/supabase-migrations.md
    - .env.example
  - Updated:
    - src/app/app.tsx
    - src/app/app-router.tsx
    - src/features/settings/components/settings-content.tsx
    - src/shared/store/sessions-store.ts
    - src/shared/store/settings-store.ts
    - src/shared/store/index.ts
    - src/shared/hooks/index.ts
    - src/shared/types/index.ts
    - src/shared/config/i18n/locales/pt-BR.ts
    - src/shared/config/i18n/locales/en-US.ts
    - src/shared/config/i18n/locales/es-ES.ts
    - docs/README.md
    - README.md
    - package.json
    - pnpm-lock.yaml
    - scripts/ralph/prd.json
    - scripts/ralph/progress.txt
- Learnings for future iterations:
  - O callback dedicado de OAuth simplifica segurança de redirecionamento e evita dependência de parsing manual em múltiplas rotas.
  - Serializar `Infinity` de DNF como `null` na nuvem e reidratar para `Infinity` no cliente mantém compatibilidade com JSON/Supabase sem quebrar regras de média.
  - O comando `pnpm knip` foi habilitado com `--no-exit-code`; o output atual aponta itens de limpeza técnica que podem ser atacados na US-012.
---
